// variables6.rs
//
// Execute `rustlings hint variables6` or use the `hint` watch subcommand for a
// hint.

/// With a constant, we must initiate the type of the variable. So here, we have an integer constant.

const NUMBER: i32 = 3;
fn main() {
    println!("Number {}", NUMBER);
}
